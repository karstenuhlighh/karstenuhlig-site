---
import Layout from "./Layout.astro";
import Nav from "../components/Nav.astro";
import { getLanguage, type Language } from "../i18n/routes";

interface Props {
  title?: string;
  description?: string;
  language?: Language;
}

const { title, description, language = getLanguage(Astro.url.pathname) } = Astro.props;
---

<Layout title={title} description={description} language={language}>
  <div class="page-shell">
    <header class="page-header">
      <Nav language={language} />
    </header>
    <main class="page-main">
      <slot />
    </main>
  </div>
</Layout>

<style>
  .page-shell {
    position: relative;
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 4rem 5rem;
  }
  @media (max-width: 768px) {
    .page-shell {
      padding: 2rem 1.5rem 3rem;
    }
  }
</style>
