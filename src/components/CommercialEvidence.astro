<section class="evidence-section" aria-labelledby="commercial-evidence-title">
  <div class="evidence-heading">
    <div class="evidence-kicker">Evidenz</div>
    <div>
      <h2 id="commercial-evidence-title">Aus Erfahrung entwickelt</h2>
      <p>Entstanden aus mehr als zwei Jahrzehnten Verantwortung entlang der digitalen Wertschöpfung, von Angebotssteuerung und Digital Marketing über CRM, Customer Journey und Data bis zur End-to-End-Steuerung eines internationalen E-Commerce-Geschäfts.</p>
    </div>
  </div>

  <a class="evidence-link" href="/track-record">Zum vollständigen Track Record <span aria-hidden="true">→</span></a>
</section>

<style>
  .evidence-section { padding: 5.25rem 0 1rem; border-top: 1px solid var(--color-border); }
  .evidence-heading { display: grid; grid-template-columns: .34fr 1fr; gap: 4rem; align-items: start; }
  .evidence-kicker { color: var(--color-amber); font-size: 11px; font-weight: 500; letter-spacing: .14em; text-transform: uppercase; }
  .evidence-heading h2 { margin: 0 0 .8rem; color: var(--color-ink); font-size: 32px; font-weight: 500; letter-spacing: -.03em; line-height: 1.15; }
  .evidence-heading p { max-width: 800px; margin: 0; color: var(--color-body); font-size: 15px; line-height: 1.75; }
  .evidence-link { display: inline-flex; gap: .45rem; align-items: center; margin-top: 2rem; color: var(--color-amber); font-size: 15px; font-weight: 500; text-decoration: none; transition: opacity .2s ease; }
  .evidence-link:hover { opacity: .7; }
  .evidence-link:focus-visible { outline: 3px solid rgba(180,83,9,.4); outline-offset: 4px; }

  @media (max-width: 820px) {
    .evidence-section { padding-top: 4rem; }
    .evidence-heading { grid-template-columns: 1fr; gap: 1rem; }
    .evidence-heading h2 { font-size: 27px; }
  }
</style>
